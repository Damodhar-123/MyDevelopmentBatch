import { CharaonlyDirective } from './charaonly.directive';

describe('CharaonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CharaonlyDirective();
    expect(directive).toBeTruthy();
  });
});
